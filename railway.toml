[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/src/server/index.js"
healthcheckPath = "/api/me"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
